using UnityEngine;
using System.Collections;

public class ｐ : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {

        if (Input.GetAxis("Horizontal")==1)
        {
            print("押された");
        }
        //if(in)
	
	}
}
